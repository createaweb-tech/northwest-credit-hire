$(function () {

    "use strict";

    // Init client-side validation. When the form is valid the browser
    // submits it natively (standard POST) — no AJAX.
    $('#ajax-contact').validator();
});
